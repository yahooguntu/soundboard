<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <script src="./dist/app.js"></script>
    <script src="./dist/vendor.js"></script>
    <link rel="stylesheet" href="./dist/vendor.css">
    <link rel="stylesheet" href="./dist/app.css">
    <title>Soundboard</title>
  </head>
  <body>
    <div class="form-group">
        <label>
          Remote/Local (N/Y):
          <input type="checkbox" name="output-selection" class="form-control output-selection">
        </label>
      </input>
    </div>
    <div class="container-fluid">
      <% @sounds.each_slice(4) do |batch| %>
        <div>
        <% batch.each do |sound| %>
          <button class="btn btn-danger btn-lg col-md-3 play-btn"><%= sound %></button>
          <audio id="<%= sound.downcase %>" controls>
            <source src="sounds/<%= sound %>" type="audio/mpeg">
          </audio>
        <% end %>
        </div>
      <% end %>
    </div>
    <form action="upload" method="post" enctype="multipart/form-data">
      <div class="form-group">
        <label>
          File for Upload
          <input type="file" name="file" class="form-control"></input>
        </label>
      </div>
      <button type="submit" class="btn btn-default">Upload</button>
    </form>
    <button class="btn btn-lg col-md-3">Update Server</button>
  </body>
</html>
